<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="10" xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.upn.gestion.controller.VistaPrestamoController"
      style="-fx-background-color: transparent;">

    <Label text="Gestión de Préstamos" styleClass="titulo-login"/>

    <TabPane tabClosingPolicy="UNAVAILABLE" style="-fx-background-color: white; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 10, 0, 0, 0);">

        <Tab text="➕ Nuevo Préstamo">
            <VBox spacing="20" style="-fx-padding: 30;">
                <Label text="Registrar Salida de Libro" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                <Separator />

                <GridPane hgap="20" vgap="15">
                    <columnConstraints>
                        <ColumnConstraints halignment="RIGHT" minWidth="120" />
                        <ColumnConstraints minWidth="300" />
                    </columnConstraints>

                    <Label styleClass="label-campo" text="Código Estudiante:" GridPane.columnIndex="0" GridPane.rowIndex="0" />
                    <TextField fx:id="txtCodigoPrestamo" promptText="Ej: N00123456" GridPane.columnIndex="1" GridPane.rowIndex="0" />

                    <Label styleClass="label-campo" text="ID del Libro:" GridPane.columnIndex="0" GridPane.rowIndex="1" />
                    <TextField fx:id="txtIdLibroPrestamo" promptText="Ingrese ID del Libro (ej: 1)" GridPane.columnIndex="1" GridPane.rowIndex="1" />

                    <Label text="Fecha Devolución:" styleClass="label-campo" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
                    <DatePicker fx:id="dpFechaDevolucion" promptText="Seleccione fecha" GridPane.rowIndex="2" GridPane.columnIndex="1"/>
                </GridPane>

                <Button text="Confirmar Préstamo" onAction="#registrarPrestamoAction"
                        styleClass="boton-ingresar" prefWidth="250"/>

                <Label fx:id="lblMensaje" style="-fx-font-size: 14px;" />
            </VBox>
        </Tab>

        <Tab text="🔄 Devoluciones / Activos">
            <VBox spacing="15" style="-fx-padding: 20;">
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <Label text="Préstamos Pendientes" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button text="🔄 Refrescar Tabla" onAction="#cargarPrestamos"/>
                </HBox>

                <TableView fx:id="tablaPrestamos" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                        <TableColumn fx:id="colEstudiante" text="Estudiante" prefWidth="200"/>
                        <TableColumn fx:id="colFecha" text="Fecha Préstamo" prefWidth="120"/>
                        <TableColumn fx:id="colVencimiento" text="Vence" prefWidth="120"/>
                        <TableColumn fx:id="colEstado" text="Estado" prefWidth="100"/>
                    </columns>
                </TableView>

                <HBox alignment="CENTER_RIGHT">
                    <Button text="📥 Registrar Devolución" onAction="#procesarDevolucionAction"
                            style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 10 20; -fx-cursor: hand;"/>
                </HBox>
            </VBox>
        </Tab>

    </TabPane>
</VBox>